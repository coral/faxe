# Rust and native build output (including builds in individual crates).
# Cargo.lock is intentionally committed for this application.
target/
/build/
/dist/
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
compile_commands.json
build.ninja
.ninja_deps
.ninja_log
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.pdb
*.dSYM/

# Machine-specific Cargo overrides and registry credentials.
/.cargo/config
/.cargo/config.toml
/.cargo/credentials
/.cargo/credentials.toml

# Local environment and signing material. Keep example configuration publishable.
.env
.env.*
!.env.example
!.env.*.example
!.env.template
!.env.*.template
/secrets/
/signing/
*.p8
*.p12
*.pfx
*.key
*.keychain
*.keychain-db
*.mobileprovision
*.provisionprofile

# Local application data. Use .faxe-local/ for a repository-local FAXE_APP_DIR.
/.faxe-local/
faxe.sqlite3
faxe.sqlite3-*
engine.lock
/settings.json
*.log

# Packaging intermediates and distributable binaries.
.flatpak-builder/
/.flatpak-vendor/
/.flatpak-cargo-config.toml
/packaging/flatpak/build/
/packaging/flatpak/repo/
/packaging/flatpak/shared-modules/
/packaging/flatpak/cargo-sources.json
/packaging/windows/stage/
/packaging/windows/identity.local.json
*.app/
*.dmg
*.pkg
*.msi
*.msix
*.msixbundle
*.appx
*.appxbundle
*.AppImage
*.appimage
*.flatpak

# Tool caches, coverage and editor/OS housekeeping.
__pycache__/
*.py[cod]
.venv/
node_modules/
.pnpm-store/
/web/dist/
.wrangler/
.dev.vars
.dev.vars.*
wrangler.local.*
wrangler.*.local.*
/coverage/
*.profraw
*.profdata
/.idea/
*.swp
*.swo
*~
.DS_Store
._*
Thumbs.db
Desktop.ini
.vscode

# licenses/ contains the generated inventory and reviewed source snapshots.
# web/public/licenses.html is the generated website acknowledgments page.
# Keep that entire directory, packaging assets, and example manifests in Git.

# Server example data and locally built Go SDK.
/faxe-server-data/
/faxe-receive-data/
/received-faxes/
/bindings/go/native/
